[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

# 如果有 API 路由，Netlify 会自动处理为函数
# 无需额外配置

# 重定向示例（如果需要）
# [[redirects]]
#   from = "/api/*"
#   to = "/.netlify/functions/:splat"
#   status = 200